{
  "name": "space-survival",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 43127",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 43127",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-43127}"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
